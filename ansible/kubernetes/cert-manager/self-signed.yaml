apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: selfsigned-issuer
  namespace: traefik
spec:
  selfSigned: {}

# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: selfsigned
#   namespace: traefik
# spec:
#   dnsNames:
#     - "*"
#   secretName: selfsigned
#   issuerRef:
#     name: selfsigned-issuer
