---
- hosts: "nodes"
  become: yes
  gather_facts: yes

  vars_files:
    - vars.yaml

  tasks:
    - name: Run post-install Proxmox script
      ansible.builtin.shell: |
        bash -c "$(wget -qLO - https://github.com/tteck/Proxmox/raw/main/misc/post-pve-install.sh)"
